/** A brief beat before the login screen — just enough to not feel like a jump cut. */
export function SplashScreen() {
  return (
    <main className="app">
      <div className="splash">
        <h1>Expedition</h1>
        <div className="splash-spinner" />
      </div>
    </main>
  )
}
